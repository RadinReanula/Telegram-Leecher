# Direct runtime dependencies — pip install -r requirements.txt
# Transitive (pulled automatically): aiohttp, aiofiles, magic-filter (aiogram);
# pyaes, pysocks (pyrogram); annotated-types, pydantic-core (pydantic)
pyrogram>=2.0.106
tgcrypto
aiogram>=3.13.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
certifi>=2024.0.0
